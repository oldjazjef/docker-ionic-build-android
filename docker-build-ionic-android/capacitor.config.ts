import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.jaz.dockerbuilder',
  appName: 'docker-builder',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
